import styled from '@mui/material/styles/styled';
import Divider from '@mui/material/Divider';

const DeviderLight = styled(Divider)({
    borderWidth: 1.5,
    borderColor: "black"
});

export default DeviderLight;